# API Configuration
VITE_API_URL=http://localhost:8000

# Development Mode
# When true: uses testnet (Base Sepolia, chain ID 84532)
# When false: uses mainnet (Base, chain ID 8453)
VITE_DEV_MODE=true

# Network Configuration (auto-set based on VITE_DEV_MODE, but can be overridden)
# VITE_NETWORK will be auto-set to "Base Sepolia (Testnet)" or "Base Mainnet"
# VITE_CHAIN_ID will be auto-set to 84532 or 8453
